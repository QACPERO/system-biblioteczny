# -----------------------------------------------------------------------------
# Globalne ignorowane pliki (przeniesione z góry, aby było kompleksowo)
# -----------------------------------------------------------------------------

# Pliki tymczasowe edytorów i systemów operacyjnych
*~                      # Ogólne pliki kopii zapasowych edytorów (np. Vim, Emacs)
.#* # Pliki kopii zapasowych (np. Emacs)
*.bak                   # Ogólne pliki kopii zapasowych
*.swp                   # Pliki wymiany Vim
*.swo                   # Pliki wymiany Vim
*.tmp                   # Ogólne pliki tymczasowe
.DS_Store               # MacOS Finder
Thumbs.db               # Windows Thumbs
ehthumbs.db             # Windows Thumbs
Desktop.ini             # Windows desktop config
*.log                   # Pliki logów

# -----------------------------------------------------------------------------
# Qt Creator & Qt specific files
# -----------------------------------------------------------------------------

# Qt Creator user settings (local to developer)
*.pro.user              # QMake project user settings
*.pro.user.* # Specific configurations (e.g., debug/release)
*.qbs.user              # QBS project user settings
*.qbs.user.*
*.creator.user          # General Qt Creator user settings
*.qmlproject.user       # QML project user settings
CMakeLists.txt.user* # CMake user settings in Qt Creator

# Files generated by Qt tools (MOC, UIC, RCC)
moc_*.cpp               # Meta-Object Compiler generated source files
moc_*.h                 # Meta-Object Compiler generated header files
ui_*.h                  # User Interface Compiler generated header files from .ui files
qrc_*.cpp               # Resource Compiler generated source files from .qrc files
*.qrc_*.cpp             # Alternative naming for resource files
object_script_*.cpp     # QMake generated object script files
*.moc                   # Old/alternative MOC output extension

# QMake/Build System Specific Files
Makefile* # Makefiles generated by qmake (e.g., Makefile, Makefile.Debug, Makefile.Release)
*.qmake.cache           # QMake cache file
*.qmake.stash           # QMake stash file
*.prl                   # QMake private library info file

# Build directories (crucial for Qt Creator projects)
# This covers directories like build-MyProject-Desktop_Qt_6_5_2_MinGW_64bit-Debug/
build*/                 # Ignoruj wszystkie katalogi zaczynające się od "build"
Debug/                  # Często używane nazwy katalogów debug/release
Release/
MinSizeRel/
RelWithDebInfo/
bin/                    # Typowe katalogi na pliki binarne
lib/                    # Typowe katalogi na biblioteki

# -----------------------------------------------------------------------------
# C/C++ Compilation Artifacts (from your list, with additions)
# -----------------------------------------------------------------------------

# Prerequisites / Dependency files
*.d                     # Dependency files (e.g., GCC -MD output)
*.dep                   # Other dependency files

# Compiled Object files
*.slo                   # Shared Library Object (specific to some build systems/platforms)
*.lo                    # Libtool Object files
*.o                     # Standard Object files
*.obj                   # Object files (Windows)

# Precompiled Headers
*.gch                   # GCC Precompiled Headers
*.pch                   # Other Precompiled Headers

# Compiled Dynamic libraries
*.so                    # Shared Objects (Linux/Unix)
*.dylib                 # Dynamic Libraries (macOS)
*.dll                   # Dynamic Link Libraries (Windows)

# Fortran module files (if applicable, less common in pure C++/Qt)
*.mod                   # Fortran module files
*.smod                  # Fortran shared module files

# Compiled Static libraries
*.lai                   # Libtool Archive Info
*.la                    # Libtool Archive (for libtool projects)
*.a                     # Standard Static Libraries (Linux/Unix)
*.lib                   # Static Libraries (Windows)

# Executables
*.exe                   # Executables (Windows)
*.out                   # Executables (Linux/Unix default)
*.app                   # macOS Application bundles (should usually ignore the whole bundle)

# -----------------------------------------------------------------------------
# Debugging & IDE specific files (common for C++ projects)
# -----------------------------------------------------------------------------

# Debugging information files
*.ilk                   # Incremental Linker database (MSVC)
*.pdb                   # Program Database (MSVC debug info)
*.idb                   # Incremental build database (MSVC)
*.bsc                   # Browse Information file (MSVC)
*.sbr                   # Source Browser information file (MSVC)
*.vcproj.*.user         # Visual Studio user files (if mixing with MSVC)
*.vcxproj.user
*.vcxproj.filters
*.sln.docstates         # Visual Studio solution states

# CMake specific files (if using CMake with Qt Creator)
CMakeCache.txt          # CMake cache file
CMakeFiles/             # CMake generated build files directory
CTestTestfile.cmake     # CTest specific file
cmake_install.cmake     # CMake install script
install_manifest.txt    # CMake install manifest
build/                  # Standard CMake build directory (already covered by build*/)
_CPack_Packages/        # CPack generated packages

# Other common build artifacts
compile_commands.json   # Generated by CMake or other tools for IDE integration (e.g., Clangd)
